import React from "react";

const Loading = () => {
	return (
		<div className="loader flex-column justify-content-center align-items-center">
			<img className="shake" src="/logo192.png" alt="loader" />
		</div>
	);
};

export default Loading;
